{
  "name": "obsidian-anki-sync",
  "version": "1.0.0",
  "description": "Two-way sync between Obsidian and Anki",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production"
  },
  "devDependencies": {
    "@types/node": "^20",
    "builtin-modules": "^3.3.0",
    "esbuild": "^0.20",
    "obsidian": "^1.5.0",
    "tslib": "^2.6",
    "typescript": "^5.3"
  }
}
